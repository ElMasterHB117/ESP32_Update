from machine import Pin
import time

led = Pin(2, Pin.OUT)

while True:
    
    for_in range(4):
        
        led.on()
        time.sleep(0.5)
        led.off()
        time.sleep(0.3)
        
        
    time.sleep(2)